import { TestBed } from '@angular/core/testing';
import { FrameworksPluginsModalService } from './frameworks-plugins-modal.service';

describe('FrameworksPluginsModalService', () => {
  let service: FrameworksPluginsModalService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(FrameworksPluginsModalService);
  });
});
