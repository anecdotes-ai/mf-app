import { UserEventDirective } from './user-event.directive';

describe('AmplitudeEventDirective', () => {
  it('should create an instance', () => {
    // const directive = new UserEventDirective();
    // expect(directive).toBeTruthy();
  });
});
